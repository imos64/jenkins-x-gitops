apiVersion: v1
kind: Service
metadata:
  name: delivery-demo
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: delivery-demo
  type: ClusterIP
